{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      2,
      "always",
      ["feat", "fix", "docs", "style", "refactor", "test", "chore", "revert","release","merge"]
    ],
    "header-max-length": [2, "always", 72],
    "type-empty": [2, "never"]
  }
}